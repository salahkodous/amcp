{
  "version": "verify-v1",
  "cases": [
    {"name": "nested-required-present", "valid": true,
     "schema": {"type": "object", "required": ["a"],
                "properties": {"a": {"type": "object", "required": ["b"],
                                     "properties": {"b": {"type": "string"}}}}},
     "instance": {"a": {"b": "x"}}},
    {"name": "nested-required-missing", "valid": false,
     "schema": {"type": "object", "required": ["a"],
                "properties": {"a": {"type": "object", "required": ["b"],
                                     "properties": {"b": {"type": "string"}}}}},
     "instance": {"a": {}}},
    {"name": "type-mismatch", "valid": false,
     "schema": {"type": "object", "properties": {"n": {"type": "integer"}}},
     "instance": {"n": "3"}},
    {"name": "bool-is-not-integer", "valid": false,
     "schema": {"type": "integer"},
     "instance": true},
    {"name": "number-accepts-int", "valid": true,
     "schema": {"type": "number"},
     "instance": 3},
    {"name": "enum-hit-and-miss", "valid": true,
     "schema": {"type": "string", "enum": ["a", "b"]},
     "instance": "b"},
    {"name": "enum-miss", "valid": false,
     "schema": {"type": "string", "enum": ["a", "b"]},
     "instance": "c"},
    {"name": "range-bounds", "valid": false,
     "schema": {"type": "number", "minimum": 0, "maximum": 100},
     "instance": 101},
    {"name": "array-items", "valid": false,
     "schema": {"type": "array", "items": {"type": "string"}},
     "instance": ["ok", 7]},
    {"name": "unknown-keywords-ignored", "valid": true,
     "schema": {"type": "object", "format": "agent-id", "$ref": "#/nope",
                "properties": {"a": {"type": "string"}}},
     "instance": {"a": "x"}},
    {"name": "unknown-type-ignored", "valid": true,
     "schema": {"type": "weird"},
     "instance": {"anything": [1]}},
    {"name": "string-length", "valid": false,
     "schema": {"type": "string", "minLength": 2, "maxLength": 4},
     "instance": "toolong"}
  ]
}
